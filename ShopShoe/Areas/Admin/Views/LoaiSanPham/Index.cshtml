@model List<ShopShoe.Models.LoaiSanPham>

@{
    ViewBag.Title = "Admin";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Loại sản phẩm</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <a href="@Url.Action("Them","LoaiSanPham")" class="btn btn-default">Thêm mới</a>
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Tên loại</th>
                            <th>Ngày tạo</th>
                            <th>Hành động</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="odd gradeX">
                                <td>@item.ID</td>
                                <td>@item.TenLoai</td>
                                <td>@item.NgayTao</td>
                                <td class="center">
                                    <a href="@Url.Action("Sua","LoaiSanPham",new {ID=item.ID})">Sửa</a> | 
                                    @Ajax.ActionLink("Xóa", "Xoa", new { @ID = item.ID }, new AjaxOptions
                               {
                                   Confirm = "Bạn có chắc chắn xóa?",
                                   OnComplete = "$('" + @item.ID + "').remove()",
                                   HttpMethod = "Delete",
                                   OnSuccess = "window.location.reload()"
                               })
                                </td>

                            </tr>
                        }



                    </tbody>
                </table>
                <!-- /.table-responsive -->

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>

</div>